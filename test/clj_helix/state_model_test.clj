(ns clj-helix.state-model-test
  (:require [clojure.test :refer :all]
            [clj-helix.state-model :refer :all]))

;(deftest state-model-class-test
;  (let [m (state-model-class (on off "off") (off on "on"))]
;    (prn m)))
